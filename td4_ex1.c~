#include <stdlib.h>
#include <stdio.h>
#include <stdarg.h>

void CatStrings(char *str1, ...)
{
  char * s;
  va_list l;
  va_start(l,s1); //init
  while((s = va_arg(l,char *)) != NULL)
    printf("%s", s);

}

int main()
{
  CatStrings("test","hello",NULL);
  return 0;
  
}
