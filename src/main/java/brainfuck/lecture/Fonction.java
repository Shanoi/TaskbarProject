package brainfuck.lecture;

/**
 * This class represents the function. It allows to create functions in a program.
 *
 * @author Team Taskbar
 */
public class Fonction {

    /*private static List<EnumCommands> list = new ArrayList<>();
    private static Queue<String> fonctionName = new LinkedList<>();
    private static Stack<Integer> retAdress = new Stack<>();

    public static addFonctionName(String str) {
        fonctionName.add(str);
    }

    public static addRetAdress(int adress) {
        retAdress.push(adress);
    }*/

}

//il faudrait pouvoir appeler directemnt les fonctions
//utilisé leur nom
// par l'intermediaire d'un enumcommand
//donc call
//à chaque call est associé une fonction
//ligne -> fonction (de la liste)


// a chaque call on crée un liste de taille 30000-list.size()
// que ce passe t-il si un left ou
// ne pas oublier de supprimer les cases à droite

//j'ai besoin de la liste d'argument donnée à call
//j'ai besoin de vérifié qu'il y a assez d'argument
//je dois stocké ces arguments dans la mem locale des foncitons

//nouvelle commande ARG pour les fonctions